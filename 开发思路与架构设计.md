# FocusForest（专注森林）— 开发思路与架构设计

> **项目定位**：一款将"番茄工作法"与"模拟经营游戏"深度结合的 Windows 桌面端 C++ 专注效率工具。
> **技术栈**：C++17 + Qt (QWidget + QSS) + Windows API + 定长二进制随机文件读写。
> **课程来源**：华南理工大学课程设计项目。
> **当前状态**：全部核心功能实现完成，进入 QA & 文档阶段。

---

## 1. 技术栈选型

在 Windows 客户端开发中，平衡开发效率、UI 美观度与底层 API 调用便利性是选型的关键。

| 维度 | Qt (QWidget/QSS) | WinUI 3 | Dear ImGui |
|:---|:---|:---|:---|
| **开发效率** | **高**（信号槽机制，GUI 拖拽与代码结合） | **中**（C++/WinRT 语法繁琐） | **高**（适合快速原型） |
| **UI 美观度** | **高**（QSS 样式表，易实现现代扁平化） | **极高**（Fluent Design） | **中**（偏向游戏风格） |
| **底层 API 整合** | **极佳**（纯 C++，无缝调用 WinAPI） | **极佳**（微软原生） | **良好** |
| **学习曲线** | **平缓**（文档丰富，适合学生团队） | **陡峭**（社区生态薄弱） | **平缓** |
| **中文生态** | **丰富** | 稀少 | 一般 |

### 实际采用方案

| 层面 | 选型 | 版本 / 说明 |
|:---|:---|:---|
| **UI 框架** | **Qt 6.5.3（QWidget + QSS）** | MinGW 64-bit，信号槽解耦 UI 与逻辑 |
| **构建系统** | **CMake** | `CMAKE_CXX_STANDARD=17`，`AUTOMOC + AUTORCC`，同时兼容 Qt5/Qt6 |
| **编译器** | **MinGW g++ 16.1 (UCRT)** | `-static-libgcc -static-libstdc++` 静态链接运行时 |
| **数据存储** | **自定义定长二进制文件** | 纯 `std::fstream` + `seekg`/`seekp`，无第三方数据库 |
| **测试框架** | **Qt Test**（QTestLib） | `BUILD_TESTS=ON` 可选编译 |
| **版本控制** | **Git** | Monorepo 结构，各模块独立目录 |

> **选型理由**：Qt QWidget 是课程项目的最佳平衡点——开发效率高、文档丰富、信号槽机制天然适合 MVC 解耦。二进制文件方案直接满足课程"随机文件处理"核心要求。CMake 比 qmake 更灵活，同时支持 Qt5/Qt6 双版本。

---

## 2. 架构设计：MVC 模式

采用经典的 **MVC (Model-View-Controller)** 模式，通过 Qt 信号槽机制实现各层解耦通信：

```
                      ┌─────────────────────────────┐
                      │           View              │
                      │  MainWindow / GardenCanvas  │
                      │  TimerRing / SettingsDialog │
                      │  StoreDialog / Statistics   │
                      │  LoginDialog / History      │
                      └──────────────┬──────────────┘
                              ▲      │
                UI 刷新信号    │      │ 用户操作（开始/暂停/放弃）
                              │      ▼
                      ┌───────┴──────────────────────┐
                      │        Controller            │
                      │      FocusController         │
                      │   (状态机 + 计时核心)         │
                      └──────┬───────────┬───────────┘
                             │           │
            状态控制 / 读写   │           │ 启停监听
                             ▼           ▼
               ┌──────────────────┐  ┌──────────────────┐
               │      Model       │  │  System Monitor  │
               │  Plants/Storage  │  │  (SystemMonitor) │
               │  Coin/Achieve    │  │  (RuleEngine)    │
               └──────────────────┘  └──────────────────┘
```

### 2.1 各层职责

| 层 | 核心类 | 职责 |
|:---|:---|:---|
| **View** | `MainWindow`, `GardenCanvas`, `TimerRing`, `SettingsDialog`, `StoreDialog`, `StatisticsDialog`, `LoginDialog`, `HistoryWidget`, `GridSelectDialog`, `AchievementToast` | 界面渲染、用户交互、信号发射 |
| **Controller** | `FocusController` | 核心状态机（Idle→Focusing→Paused→Success/Failed），计时器驱动，协调 Model 与 View，违规回调入口 |
| **Model** | `AbstractPlant` 继承体系, `FocusSession`, `FocusRecord`, `CoinManager`, `AchievementEngine`, `StatisticsCalculator` | 数据与状态维护，植物多态生长，金币/成就逻辑 |
| **Data** | `DatabaseManager`, `PresetManager`, `UserManager` | 定长二进制文件 CRUD，`seekg`/`seekp` 随机读写 |
| **Monitor** | `SystemMonitor`, `RuleEngine` | Windows API 前台窗口检测，黑白名单过滤，大小写不敏感匹配 |

---

## 3. 核心类继承体系

满足课程要求：**5 个以上类、2 层以上继承、多态使用**。

### 3.1 植物体系（3 层继承）

```
                    AbstractPlant（第一层，抽象基类）
                    ├── virtual ~AbstractPlant() = default
                    ├── virtual void grow() = 0
                    ├── virtual void wither() = 0
                    ├── virtual QChar displayChar() = 0
                    ├── GrowthStage: Seed→Sprout→Growing→Mature→Withered
                    │
            ┌───────┴────────┐
            │                │
         Tree（第二层）     Flower（第二层）
         + canopySize       + color
            │                │
      ┌─────┴─────┐     ┌───┴─────┐
      │           │     │         │
   OakTree    PineTree  Rose   Sunflower
   (第三层)   (第三层)  (第三层) (第三层)
```

### 3.2 全部类清单（18 个核心类）

| 层 | 类名 | 继承 | 说明 |
|:---|:---|:---|:---|
| Plant | `AbstractPlant` | — | 抽象基类，定义 `grow()`/`wither()` 纯虚接口 |
| Plant | `Tree` | `AbstractPlant` | 树木抽象类，增加 `canopySize` |
| Plant | `Flower` | `AbstractPlant` | 花卉抽象类，增加 `color` |
| Plant | `OakTree` | `Tree` | 橡树（3 层），生长速度中等 |
| Plant | `PineTree` | `Tree` | 松树（3 层），生长较慢，耐寒寓意 |
| Plant | `Rose` | `Flower` | 玫瑰（3 层），红色，生长较慢 |
| Plant | `Sunflower` | `Flower` | 向日葵（3 层），黄色，生长较快 |
| Model | `FocusSession` | `QObject` | 专注会话模型，状态机 + 倒计时 |
| Model | `FocusRecord` | — | 定长 64 字节记录结构体 |
| Controller | `FocusController` | `QObject` | 核心控制器，状态机 + 计时驱动 |
| Data | `DatabaseManager` | — | 二进制文件 CRUD，`seekg`/`seekp` |
| Data | `UserManager` | — | 多账号系统，FNV-1a 加盐哈希 |
| Data | `PresetManager` | — | 快捷预设管理，3 组出厂预设 |
| Monitor | `SystemMonitor` | `QObject` | WinAPI 前台窗口检测 |
| Monitor | `RuleEngine` | — | 黑白名单规则引擎 |
| Logic | `CoinManager` | `QObject` | 金币系统，二进制持久化 |
| Logic | `AchievementEngine` | — | 成就系统，位图算法 |
| Logic | `StatisticsCalculator` | — | 标签时间统计 |

**继承层次验证**：`AbstractPlant → Tree → OakTree`，共 3 层，≥2 层 ✓。类总数 18，≥5 类 ✓。

---

## 4. 数据结构设计

### 4.1 定长二进制记录 (`FocusRecord`)

每条专注记录固定 **64 字节**，通过 `#pragma pack(1)` 消除对齐填充：

```cpp
#pragma pack(push, 1)
struct FocusRecord {
    int32_t  recordId;           // 4B    记录唯一标识（自增 ID）
    char     plantType[32];      // 32B   植物类型名称
    int32_t  durationSeconds;    // 4B    计划专注时长（秒）
    int32_t  actualSeconds;      // 4B    实际专注时长（秒）
    int64_t  timestamp;          // 8B    Unix 时间戳
    uint8_t  isSuccess;          // 1B    是否成功（1=成功, 0=失败）
    char     reserve[15];        // 15B   预留，补齐至 64 字节
};
static_assert(sizeof(FocusRecord) == 64, "Must be exactly 64 bytes");
#pragma pack(pop)
```

**随机访问公式**：第 N 条记录起始偏移 = `N × 64`，直接 `seekg`/`seekp` 定位。

---

## 5. Windows API 监听策略

### 5.1 实际采用方案：QTimer 主线程轮询

```cpp
// SystemMonitor.cpp 核心流程
#define NOMINMAX              // 阻止 windows.h 定义 min/max 宏
#define WIN32_LEAN_AND_MEAN   // 只加载常用 API，加快编译
#include <windows.h>
#include <psapi.h>

// 每 500ms 轮询一次
QTimer* timer = new QTimer(this);
connect(timer, &QTimer::timeout, this, &SystemMonitor::pollForeground);
timer->start(500);

void pollForeground() {
    HWND hwnd = GetForegroundWindow();
    DWORD pid = 0;
    GetWindowThreadProcessId(hwnd, &pid);

    // 关键避坑：使用 PROCESS_QUERY_LIMITED_INFORMATION 而非 PROCESS_VM_READ
    // 避免对管理员权限窗口的 OpenProcess 报 ERROR_ACCESS_DENIED（错误码 5）
    HANDLE hProcess = OpenProcess(PROCESS_QUERY_LIMITED_INFORMATION, FALSE, pid);
    if (hProcess) {
        wchar_t path[MAX_PATH];
        if (QueryFullProcessImageNameW(hProcess, 0, path, &size)) {
            // 提取 exe 文件名 → 送 RuleEngine 黑白名单匹配
        }
        CloseHandle(hProcess);
    }
}
```

| 技术点 | 选择 | 原因 |
|:---|:---|:---|
| 检测方式 | **QTimer 轮询**（500ms） | `GetForegroundWindow()` 单次调用仅微秒级，对 UI 零影响 |
| 线程模型 | **主线程** | 完全避免 `QThread`/`std::mutex`/跨线程信号复杂性 |
| 进程权限 | `PROCESS_QUERY_LIMITED_INFORMATION` | Vista+ 可用，无需管理员权限获取高权限进程名 |
| 宏污染防护 | `NOMINMAX` + `WIN32_LEAN_AND_MEAN` | 在 `#include <windows.h>` 之前定义 |

### 5.2 备选方案：SetWinEventHook 事件钩子（未采用）

利用 `SetWinEventHook` 监听 `EVENT_SYSTEM_FOREGROUND` 事件，无需定时轮询。**未采用原因**：需要独立线程处理回调 + 跨线程信号投递，复杂度远高于 QTimer 方案，学生项目不必要。

---

## 6. 进阶功能体系

以下功能在核心 MVC 架构基础上扩展实现，全部已完成：

### 6.1 双专注模式

| 模式 | 行为 | 违规惩罚 |
|:---|:---|:---|
| **严格模式** (STRICT) | 检测到黑名单进程 → 10s 全屏警告倒计时 → 期内切回免罚 → 超时枯萎 | 植株枯萎，无金币 |
| **温和模式** (GENTLE) | 切屏不判定枯萎 | 专注成功后金币折半（50%） |

### 6.2 多账号系统

- `UserRecord` 64B 定长结构体，FNV-1a 加盐哈希存储密码
- 每个账号独立数据沙箱：`app_data/user_{id}/sessions.dat`、`coins.dat`
- `LoginDialog` 登录/注册界面

### 6.3 金币与商城

- 专注成功获得金币：`earned = actualSeconds / 300`（每 5 分钟 = 1 金币）
- `CoinManager` 管理金币余额，持久化到 `coins.dat`
- `StoreDialog` 展示 5 种可解锁植物（500-1000 金币），卡片式布局

### 6.4 成就系统

- `AchievementEngine`：位图算法追踪 5 项成就
- `AchievementToast`：金边淡出动画弹出提示

### 6.5 其他功能

| 功能 | 核心类 | 说明 |
|:---|:---|:---|
| 秒表模式（正计时） | `FocusController::TimerMode::STOPWATCH` | `plannedMinutes=0` 哨兵值，正向累加 |
| 标签分类统计 | `StatisticsCalculator` + `PieChartWidget` | QPainter 手绘甜甜圈饼图 |
| 快捷预设 | `PresetManager` | 3 组出厂预设 + 卡片式一键启动 |
| 自勉语录 | `QuoteProvider` | 16 条随机中文语录 |
| 时间历程 | `HistoryWidget` + `TimelineItemWidget` | QPainter 手绘纵向时间轴 |
| 树梢誓言 | `PlantTimerWidget` | 用户自定义目标宣言，专注中显示 |
| 8×8 自选网格 | `GridSelectDialog` + `gridIndex` 字段 | 自定义森林画布种植位置 |
| 可拖动旋钮设时 | `PlantTimerWidget` | 圆环拖动设时 10-120min |

---

## 7. 核心设计决策与避坑记录

| # | 决策点 | 选择 | 原因 / 避坑说明 |
|:---|:---|:---|:---|
| 1 | 植物指针管理 | `std::unique_ptr<AbstractPlant>` | C++17 最佳实践，自动释放，杜绝悬空指针和双重释放 |
| 2 | 状态枚举 | `enum class State : uint32_t` | 强类型，防止命名污染，编译期检查 |
| 3 | 虚析构函数 | `virtual ~AbstractPlant() = default` | **必须声明**，否则 `delete` 基类指针时子类析构函数不被调用 → 内存泄漏 |
| 4 | fstream 状态位 | 每次 I/O 前 `file_.clear()` | EOF/failbit 置起后 `write()` 被静默忽略，**极易被忽视的 C++ 陷阱** |
| 5 | 数据文件路径 | `QCoreApplication::applicationDirPath()` | 严禁硬编码绝对路径，确保跨机器可运行 |
| 6 | `<windows.h>` 宏污染 | `.cpp` 中 `NOMINMAX` + `WIN32_LEAN_AND_MEAN` | 阻止 `min`/`max` 宏覆盖 `std::min`/`std::max`；**绝不在 .h 中包含 `<windows.h>`** |
| 7 | 进程名匹配 | `Qt::CaseInsensitive` | Windows 文件名不区分大小写，精确匹配会导致漏检 |
| 8 | 崩溃恢复 | 最后一条记录 `isSuccess==0` + 状态判定 | 启动时检测 → 自动追加枯萎记录，利用定长文件本身作为状态标志 |
| 9 | MinGW ABI | `-static-libgcc -static-libstdc++` + `--no-compiler-runtime` | 避免 UCRT/MSVCRT DLL 版本冲突导致静默闪退 |
| 10 | 图片资源 | Qt `.qrc` 资源文件，`:/` 路径 | 编译期嵌入 exe，不依赖磁盘路径，确保老师运行不丢图 |
| 11 | QPainter 缓存 | 构造函数中一次性加载 `QPixmap` | `paintEvent` 高频触发，零 I/O 开销，避免卡顿 |
| 12 | MOC 编译 | 含 `Q_OBJECT` 的 `.cpp` 必须加入 `add_executable` | 否则 MOC 不处理 → 链接阶段 LNK2019（无法解析的外部符号） |

---

## 8. 项目目录结构与模块职能

```
forest_co/
├── 开发思路与架构设计.md              ← 本文档
├── Forest_单人敏捷开发计划.md          ← 完整 WBS 开发计划与每周完成状态
│
├── Data Storage Module Development/    ← 成员3：纯 C++ 定长二进制存储引擎
│   ├── DatabaseCommon.h               → FocusRecord 64B 结构体定义（#pragma pack）
│   ├── DatabaseManager.h/.cpp         → CRUD 接口（append/readById/updateById/count）
│   └── main.cpp                       → 控制台 7 步验证程序
│
├── member2_plant_module/               ← 成员2：OOP 植物继承体系 + 状态机核心
│   ├── include/plant/                 → AbstractPlant → Tree → OakTree/PineTree
│   │                                 →                → Flower → Rose/Sunflower
│   ├── include/plant/PlantFactory.h   → 工厂模式（std::map 注册创建）
│   ├── include/controller/
│   │   └── FocusController.h          → 状态机（IDLE→FOCUSING→SUCCESS/FAILED）
│   ├── src/                           → 各 .cpp 实现
│   └── main.cpp                       → 控制台集成验证
│
└── member_4_winmonitor/               ← 成员4：系统监控 + UI + 集成（实际集大成者）
    ├── CMakeLists.txt                 → Qt5/Qt6 双版本兼容，CMake 构建
    ├── src/core/                      → AbstractPlant, FocusController, FocusSession,
    │                                     FocusRecord, CoinManager, AchievementEngine, etc.
    ├── src/data/                      → RandomAccessDatabase（fstream + seekp/seekg）
    ├── src/monitor/                   → SystemMonitor（WinAPI 前台窗口检测）
    ├── src/ui/                        → MainWindow, GardenCanvas, TimerRing,
    │                                     SettingsDialog, StoreDialog, StatisticsDialog,
    │                                     LoginDialog, HistoryWidget, GridSelectDialog, etc.
    └── tests/test_main.cpp            → 29 个测试用例（功能+集成+边界）
```

> **注意**：当前仓库存在两套植物体系（member2 用 `grow(int minutes)`，member4 用 `grow()` 秒级递增），以及两套数据库实现（`DatabaseManager` vs `RandomAccessDatabase`）。**member_4_winmonitor 是事实上的集成版本**，包含了 UI、Monitor、完整测试。后续如需统一，应以 member4 的方案为标准。

---

## 9. 开发进度跟踪

### 9.1 各模块完成状态

| 模块 | 负责目录 | 核心功能 | 状态 |
|:---|:---|:---|:---:|
| 存储引擎 | `Data Storage Module Development` | 定长二进制 CRUD，`seekg`/`seekp` 随机读写 | ✅ |
| 植物体系 | `member2_plant_module` | 3 层继承 + 工厂模式 + 控制台验证 | ✅ |
| 状态机 | `member2_plant_module` | FocusController 五态流转 + 多态调用 | ✅ |
| 系统监控 | `member_4_winmonitor` | WinAPI `GetForegroundWindow` + 黑白名单 | ✅ |
| UI 界面 | `member_4_winmonitor` | MainWindow + QSS 深林绿主题 + 抽屉导航 | ✅ |
| 森林画布 | `member_4_winmonitor` | GardenCanvas M×N 网格 + 8×8 自选位 | ✅ |
| 金币商城 | `member_4_winmonitor` | CoinManager + StoreDialog 卡片式布局 | ✅ |
| 多账号 | `member_4_winmonitor` | UserManager + 数据沙箱 | ✅ |
| 成就系统 | `member_4_winmonitor` | 位图算法 + Toast 动画 | ✅ |
| 统计图表 | `member_4_winmonitor` | QPainter 甜甜圈饼图 | ✅ |
| 测试 | `member_4_winmonitor/tests` | 29 个测试用例（功能+集成+边界） | ✅ |
| 编译验证 | `member_4_winmonitor` | MinGW g++ 16.1 编译通过，0 错误 | ✅ |
| 便携打包 | `member_4_winmonitor` | `windeployqt` 绿色便携包（~23MB） | ✅ |
| 双文档 | 全局 | 开题报告 (~1.8k字) + 结题报告 (~4k字) | ✅ |

### 9.2 里程碑对照

| 阶段 | 内容 | 原计划 | 实际状态 |
|:---|:---|---:|:---:|
| Phase 1: MVP | Qt 窗口 + 倒计时 + 轮询监听 + 二进制写入 | 第 1 周 | ✅ 3 个模块并行完成 |
| Phase 2: 基础完善 | OOP 继承体系 + 随机读写 + 森林画布 + 黑白名单 UI | 第 2-3 周 | ✅ |
| Phase 3: 商业体验 | QSS 美化 + 动画 + 测试 + 文档 | 第 4-5 周 | ✅ |
| Phase 4: 进阶 | 多账号/成就/商城/统计/秒表/预设/网格/旋钮 | — | ✅ 已全部实现 |

---

## 10. 合规自查表

| # | 检查项 | 要求 | 实际 | ✓ |
|:---|:---|:---|:---|:---:|
| 1 | 总代码行数 | ≥2000 行 | ~4800 行（3 模块合计） | ✅ |
| 2 | 类总数 | ≥5 个 | 18 个核心类 | ✅ |
| 3 | 继承层次 | ≥2 层 | 3 层（AbstractPlant→Tree→OakTree） | ✅ |
| 4 | 多态使用 | 虚函数 + 基类指针 | `FocusController::currentPlant_->grow()` | ✅ |
| 4.1 | 虚析构函数 | `virtual ~AbstractPlant() = default` | 已声明 | ✅ |
| 5 | 定长二进制随机读写 | `seekg`/`seekp` + `read`/`write` | `DatabaseManager` 完整 CRUD | ✅ |
| 6 | 二进制文件更新 | 原地更新 | `updateById(recordId, newData)` | ✅ |
| 7 | Windows API 调用 | ≥2 个不同 API | `GetForegroundWindow` + `QueryFullProcessImageNameW` | ✅ |
| 7.1 | 宏污染防护 | `NOMINMAX` + `WIN32_LEAN_AND_MEAN` | `SystemMonitor.cpp` 顶部 | ✅ |
| 8 | 进程权限安全 | `PROCESS_QUERY_LIMITED_INFORMATION` | 不含 `PROCESS_VM_READ` | ✅ |
| 9 | 函数头注释 | Doxygen 格式 | 核心 .h 公有方法 | ✅ |
| 10 | UI 界面 | Qt Widget | 抽屉式导航 + 深林绿 QSS 主题 | ✅ |
| 11 | 测试用例 | ≥15 个 | 29 个 | ✅ |
| 12 | 无第三方数据库 | 禁用 SQLite | 纯 `fstream` | ✅ |
| 13 | 编译零警告 | `-Wall -Wextra` | ✅ MinGW g++ 16.1 | ✅ |
| 14 | 动态路径解析 | `applicationDirPath()` | ✅ | ✅ |
| 15 | 便携打包 | `windeployqt` | `Forest_Portable.zip` (~23MB) | ✅ |
| 16 | 双文档 | Proposal + Final Report | 开题 1.8k 字 + 结题 4k 字 | ✅ |

---

## 11. 5 人一个月敏捷开发计划表

本节为 **5 人协作、1 个月、4 个 Sprint** 的开发计划。计划目标不是从零推翻当前成果，而是利用仓库已有的三条成果线进行收敛：

| 当前目录 | 已有成果 | 在新计划中的定位 |
|:---|:---|:---|
| `Data Storage Module Development` | `DatabaseCommon.h`、`DatabaseManager.h/.cpp`、控制台验证程序、`forest_data.db` | 成员 2 的 Data 层基础，可作为随机文件读写课程要求的主要证明 |
| `member2_plant_module` | 植物三层继承体系、`PlantFactory`、`FocusController` 控制台验证 | 成员 3 的 Model + Controller 层基础，可补充 OOP、多态和状态机说明 |
| `member_4_winmonitor` | WinAPI 监控、Qt UI、森林画布、测试用例、CMake 集成 | 当前事实上的集成版本，后续以此目录作为主线收敛 |
| `开发思路与架构设计.md` / `Forest_单人敏捷开发计划.md` | 架构、WBS、合规自查、完成状态 | 成员 1 维护架构与集成说明，成员 5 汇总 UI 截图和测试材料 |

### 11.1 项目约束与交付目标

| 项目项 | 内容 |
|:---|:---|
| 团队规模 | 5 人协作开发 |
| 开发周期 | 1 个月，共 4 周，每周一个 Sprint |
| 技术栈 | C++17 + Qt 6.5.3（QWidget + QSS）+ CMake + Windows API + 定长二进制随机文件读写 + Qt Test + Git |
| 架构模式 | MVC 分层架构，使用 Qt 信号槽实现 View、Controller、Model、Data、Monitor 各层解耦 |
| 交付目标 | 可运行桌面程序、源码包、便携包、开题报告、结题报告、测试报告、演示脚本 |
| 功能目标 | 专注计时、植物生长、违规枯萎、森林画布、历史记录、金币商城、标签统计、测试与打包 |

### 11.2 团队角色分工表

| 成员 | 角色定位 | 主要负责模块 | 核心职责 | 主要产出物 |
|:---:|:---|:---|:---|:---|
| 成员 1 | 项目负责人 / 架构集成 / 构建发布 | 项目架构、CMake、Git、集成、打包 | 统一 MVC 架构、制定接口规范、维护主分支、组织每周集成、处理模块冲突、最终打包发布 | `CMakeLists.txt`、目录结构、接口约定文档、集成版本、`Forest_Portable.zip` |
| 成员 2 | 数据存储与用户数据负责人 | Data 层、二进制文件、账号/预设/金币数据 | 实现 64B 定长记录、随机读写、数据 CRUD、崩溃恢复、金币/预设持久化、数据测试 | `DatabaseCommon.h`、`DatabaseManager.h/.cpp`、`UserManager`、`PresetManager`、数据测试 |
| 成员 3 | 核心逻辑与植物体系负责人 | Model + Controller 层 | 实现植物继承体系、植物工厂、专注状态机、计时逻辑、金币/成就/语录等业务逻辑 | `AbstractPlant`、`Tree`、`Flower`、`OakTree`、`PineTree`、`Rose`、`Sunflower`、`FocusController`、`CoinManager`、`QuoteProvider` |
| 成员 4 | Windows 系统监听与反作弊负责人 | Monitor 层、RuleEngine、WinAPI | 实现前台窗口监听、黑白名单规则、违规检测、权限避坑、关闭拦截联动、异常退出判定 | `SystemMonitor.h/.cpp`、`RuleEngine.h/.cpp`、违规检测测试、反作弊说明 |
| 成员 5 | UI/UX 与展示交互负责人 | View 层、QWidget、QSS | 实现主窗口、计时圆环、森林画布、设置页、商城页、历史页、统计页、整体样式美化 | `MainWindow`、`TimerRing`、`GardenCanvas`、`SettingsDialog`、`StoreDialog`、`HistoryWidget`、`StatisticsDialog`、`style.qss` |

### 11.3 一个月总体里程碑

| 周次 | Sprint 主题 | 主要目标 | 阶段性验收标准 |
|:---:|:---|:---|:---|
| 第 1 周 | 项目骨架 + 接口冻结 | 搭建工程、明确模块边界、完成核心数据结构和类接口设计 | 项目可编译；目录结构稳定；64B 记录结构、植物接口、状态机接口、UI 原型和 WinAPI 验证完成 |
| 第 2 周 | MVP 核心功能开发 | 并行实现存储、植物、状态机、系统监听和基础 UI | 能完成“开始专注 → 倒计时 → 成功/失败 → 写入记录”的最小闭环 |
| 第 3 周 | 全功能联调 + 体验完善 | 完成森林画布、历史统计、金币商城、设置、黑白名单、UI 样式和模块集成 | 能完整演示“选树种植 → 违规枯萎 / 成功成熟 → 金币变化 → 森林画布展示 → 历史统计” |
| 第 4 周 | 测试修复 + 文档打包 | 完成功能测试、边界测试、代码注释、结题报告、便携打包 | 通过 ≥15 个测试用例；Release 版本可运行；完成源码包、便携包、测试报告和结题报告 |

### 11.4 每周详细交付表

| 时间 | 交付物 | 负责人 | 协作人 | 状态说明 |
|:---|:---|:---:|:---:|:---|
| 第 1 周 D1-D2 | 项目目录规范、统一 CMake、模块接口草案 | 成员 1 | 全员 | 当前仓库已有多模块目录，后续应以 `member_4_winmonitor` 为集成主线 |
| 第 1 周 D3-D5 | 植物继承体系、定长记录、WinAPI 监听原型 | 成员 2/3/4 | 成员 5 | 当前 `member2_plant_module`、`Data Storage Module Development` 已覆盖该阶段 |
| 第 2 周 D1-D3 | `FocusController` + `DatabaseManager` + `SystemMonitor` 信号链路 | 成员 1/2/3/4 | 成员 5 | 重点处理接口差异：`DatabaseManager` 与 `RandomAccessDatabase` 二选一收敛 |
| 第 2 周 D4-D5 | MVP 演示：专注成功/违规失败均可写入历史 | 成员 1 | 全员 | 验收时必须能看到二进制文件记录变化与 UI 刷新 |
| 第 3 周 D1-D2 | 商城、金币、标签统计、历史时间轴 | 成员 1/2/3 | 成员 5 | 当前架构文档记录这些功能已在集成版完成，后续以复核为主 |
| 第 3 周 D3-D5 | 多账号、成就、预设、秒表、8×8 网格、旋钮设时 | 成员 1/2/3 | 成员 4/5 | 进阶功能应逐项做冒烟测试，避免只写文档不验证 |
| 第 4 周 D1-D2 | 全量测试、边界测试、崩溃恢复与权限窗口验证 | 全体 | 成员 5 汇总 | 覆盖空文件、损坏文件、超长进程名、快速点击、管理员窗口 |
| 第 4 周 D3-D4 | Release 编译、绿色便携包、无 Qt 环境沙箱测试 | 成员 1 | 成员 4/5 | 重点检查 `--no-compiler-runtime`，避免 DLL 版本污染 |
| 第 4 周 D5 | 最终报告、演示脚本、答辩 Q&A 备忘 | 成员 1 | 全员 | 演示路线固定为：登录 → 设置 → 专注 → 违规/成功 → 历史/统计/商城 |

### 11.5 任务依赖与集成顺序

| 依赖链 | 说明 | 集成检查点 |
|:---|:---|:---|
| 存储结构 → 专注记录 → 森林画布 | `FocusRecord` 字段必须先稳定，否则 UI 历史渲染会反复改 | 记录大小固定 64B，随机读写偏移正确 |
| 植物工厂 → 状态机 → UI 展示 | UI 不直接创建具体植物，由控制器和工厂统一创建 | 基类指针调用虚函数，析构安全 |
| 系统监听 → 违规信号 → 状态机失败 | WinAPI 只负责检测，失败判定交给 `FocusController` | 黑名单命中后只写一条失败记录 |
| 金币/成就 → 专注成功事件 | 金币和成就只在成功结算后触发，失败不奖励 | 重复点击完成不会重复入账 |
| 测试 → 打包 → 沙箱验证 | 先测试逻辑，再打包，再在干净环境验证 | 便携包解压后双击即可运行 |

### 11.6 Sprint 详细任务摘要

| Sprint | 成员 1：架构集成 | 成员 2：数据存储 | 成员 3：核心逻辑 | 成员 4：系统监听 | 成员 5：UI/UX |
|:---:|:---|:---|:---|:---|:---|
| 第 1 周 | 工程骨架、CMake、Git 规范、接口冻结 | `FocusRecord` 64B 结构、`DatabaseManager` 接口 | 植物继承体系草案、状态机信号槽接口 | WinAPI Demo、`RuleEngine` 规则设计 | UI 原型、`resources.qrc` 资源规划 |
| 第 2 周 | Qt Test 框架、MVP 联调 | 二进制 CRUD、空文件/越界保护 | 植物类、植物工厂、`FocusController` | `SystemMonitor`、黑白名单判断 | `MainWindow`、`TimerRing` |
| 第 3 周 | 全链路信号槽集成、演示版本管理 | 崩溃恢复、金币/配置持久化、1000 条压力测试 | 金币商城逻辑、语录、成就、标签统计 | 违规联动、管理员窗口等边界处理 | 森林画布、设置、商城、历史、统计、QSS |
| 第 4 周 | Release 构建、`windeployqt`、演示准备 | 存储测试、数据模块文档 | 业务逻辑测试、类图和状态机说明 | 系统监听测试、反作弊技术说明 | UI 测试、截图、测试报告整理 |

### 11.7 模块甘特计划表

| 模块 / 成员 | 第 1 周 | 第 2 周 | 第 3 周 | 第 4 周 | 最终交付 |
|:---|:---|:---|:---|:---|:---|
| 成员 1：架构集成 | 工程骨架、CMake、分支规范、接口冻结 | 测试框架、MVP 集成 | 全链路集成、Demo 分支 | Release 构建、便携打包、演示准备 | 可运行集成版本、便携包 |
| 成员 2：数据存储 | 64B 记录结构、Database 接口 | 二进制 CRUD、随机读写测试 | 崩溃恢复、金币/配置持久化、压力测试 | 存储测试、数据模块文档 | 稳定 Data 层 |
| 成员 3：核心逻辑 | 植物接口、状态机接口 | 植物继承体系、植物工厂、`FocusController` | 金币、商城逻辑、成就、语录、统计计算 | 业务测试、类图和状态机文档 | 完整 Model + Controller |
| 成员 4：系统监听 | WinAPI Demo、`RuleEngine` 设计 | `SystemMonitor`、黑白名单判断 | 违规联动、边界处理、反作弊完善 | 系统监听测试、技术难点文档 | 稳定 Monitor 层 |
| 成员 5：UI/UX | UI 原型、资源系统 | `MainWindow`、`TimerRing` | `GardenCanvas`、Settings、Store、History、Statistics、QSS | UI 测试、截图、测试报告整理 | 完整 View 层 |

### 11.8 每周验收会议安排

| 时间 | 会议类型 | 参与人 | 内容 | 产出 |
|:---|:---|:---|:---|:---|
| 每周一 | Sprint Planning | 全体 | 确认本周任务、接口变更、风险点 | 本周任务板 |
| 每周三 | Mid-Sprint Sync | 全体 | 检查阻塞问题、接口冲突、集成风险 | 问题清单 |
| 每周五 | Sprint Review | 全体 | 演示本周可运行成果，确认是否进入下一阶段 | 周验收记录 |
| 每周日 | Bug Bash / 文档补齐 | 全体 | 集中修 Bug、补测试、补文档 | Bug 修复记录、文档增量 |

### 11.9 测试计划表

| 测试类别 | 负责人 | 测试内容 | 最低用例数 | 输出 |
|:---|:---|:---|---:|:---|
| 存储测试 | 成员 2 | 写入、读取、修改、越界、空文件、损坏文件、1000 条压力测试 | 6 | 存储测试记录 |
| 业务逻辑测试 | 成员 3 | 植物多态、状态机、暂停恢复、成功失败、金币、商城、语录 | 6 | 逻辑测试记录 |
| 系统监听测试 | 成员 4 | 黑名单、白名单、大小写、管理员窗口、超长进程名、异常退出 | 5 | 监听测试记录 |
| UI 测试 | 成员 5 | 主窗口、画布、设置、商城、历史、统计、弹窗、QSS | 6 | UI 测试截图 |
| 集成测试 | 成员 1 + 全体 | 完整专注流程、违规流程、重启恢复、便携包运行 | 5 | 集成测试记录 |
| 文档验收 | 成员 1 + 全体 | 开题/结题报告、测试报告、演示脚本、源码说明 | 4 | 文档检查表 |

### 11.10 最终交付清单

| 交付物 | 负责人 | 内容要求 | 是否必须 |
|:---|:---|:---|:---:|
| `Forest.exe` | 成员 1 | Release 构建生成的可执行程序 | 是 |
| `Forest_Portable.zip` | 成员 1 | 包含 exe、Qt 运行依赖、资源文件，解压即用 | 是 |
| `Forest_SourceCode.zip` | 成员 1 | 包含 `src/`、`docs/`、`CMakeLists.txt`、`resources.qrc`、`.gitignore`，排除 `build/` 和 `Qt/` | 是 |
| `docs/proposal.md` | 全体 | 开题报告，说明背景、技术路线、分工、架构 | 是 |
| `docs/final_report.md` | 全体 | 结题报告，不少于 3000 字 | 是 |
| `docs/test_report.md` | 成员 5 汇总，全体提供 | 不少于 15 个测试用例，包含功能和边界测试 | 是 |
| `docs/demo_script.md` | 成员 1 | 演示路线、讲解顺序、常见问答 | 建议 |
| 源码注释 | 全体 | 所有核心 `.h` 公有方法使用 Doxygen 风格注释 | 是 |
| 截图材料 | 成员 5 | 主界面、森林画布、商城、统计、违规弹窗、测试结果截图 | 建议 |

### 11.11 风险与缓冲策略

| 风险 | 影响 | 负责人 | 缓冲策略 |
|:---|:---|:---|:---|
| 二进制文件读写错位 | 历史记录、画布、统计全部异常 | 成员 2 | 第 2 周前完成 5 条记录读写和 1000 条压力测试；写入前统一 `file_.clear()` |
| WinAPI 获取高权限进程失败 | 反作弊功能失效 | 成员 4 | 使用 `PROCESS_QUERY_LIMITED_INFORMATION`，不申请 `PROCESS_VM_READ` |
| `windows.h` 宏污染 | 编译错误或命名冲突 | 成员 4 | 只在 `.cpp` 中包含 `windows.h`，并提前定义 `NOMINMAX`、`WIN32_LEAN_AND_MEAN` |
| Qt MOC 未处理 `Q_OBJECT` | 链接阶段报错 | 成员 1 | 所有含 `Q_OBJECT` 的类都加入 `add_executable` 源文件列表 |
| UI 与逻辑接口不一致 | 集成延期 | 成员 1、成员 3、成员 5 | 第 1 周冻结 `FocusController` 信号槽接口，第 2 周只允许小改 |
| 图片资源丢失 | 老师电脑运行白图 | 成员 5 | 使用 `.qrc` 和 `:/images/...` 路径，禁止依赖相对磁盘图片 |
| 打包后缺 DLL | 交付程序无法运行 | 成员 1 | 第 4 周前半提前跑 `windeployqt`，在干净目录中双击验证 |
| 文档最后堆积 | 结题压力过大 | 全体 | 每周将技术难点和测试截图同步写入 `docs/notes.md` |

### 11.12 工时汇总表

| 成员 | 第 1 周 | 第 2 周 | 第 3 周 | 第 4 周 | 合计 |
|:---|---:|---:|---:|---:|---:|
| 成员 1：架构集成 | 7h | 11h | 8h | 15h | 41h |
| 成员 2：数据存储 | 7h | 10h | 12h | 8h | 37h |
| 成员 3：核心逻辑 | 8h | 16h | 12h | 8h | 44h |
| 成员 4：系统监听 | 7h | 10h | 9h | 8h | 34h |
| 成员 5：UI/UX | 6h | 10h | 22h | 10h | 48h |
| **总计** | **35h** | **57h** | **63h** | **49h** | **204h** |

### 11.13 最终成功标准

| 类别 | 标准 |
|:---|:---|
| 功能完整性 | 支持专注开始、暂停、放弃、成功、违规失败、植物生长、金币奖励、商城解锁、历史记录、森林画布、标签统计 |
| 技术合规 | 使用 C++17、Qt QWidget、Windows API、定长二进制随机文件读写，不使用 SQLite/MySQL 等第三方数据库 |
| OOP 要求 | 类数量不少于 5 个；继承层次不少于 2 层；使用虚函数和基类指针实现多态 |
| 数据要求 | 每条专注记录固定 64 字节；支持 `seekg`/`seekp` 随机读取和原地更新 |
| 系统监听 | 使用 Windows API 获取前台窗口进程名；支持黑白名单和大小写不敏感匹配 |
| UI 要求 | 主界面、森林画布、设置页、商城页、历史页、统计页可正常使用，界面风格统一 |
| 测试要求 | 不少于 15 个测试用例，覆盖功能测试、边界测试和集成测试 |
| 文档要求 | 完成开题报告、结题报告、测试报告、演示脚本或说明文档 |
| 交付要求 | 提供源码包和便携运行包，便携包解压后可直接双击运行 |

### 11.14 计划与当前仓库的对应关系

| 计划模块 | 当前可参考成果 | 后续处理 |
|:---|:---|:---|
| Data 层 | `Data Storage Module Development`、`member_4_winmonitor/src/data` | 以 64B 定长随机文件读写作为报告主线，集成版保留现有实现 |
| Model + Controller | `member2_plant_module`、`member_4_winmonitor/src/core` | 用 member2 说明继承和多态，用 member4 作为最终集成运行版本 |
| Monitor 层 | `member_4_winmonitor/src/monitor` | 保留 QTimer 主线程轮询方案，强调权限和宏污染避坑 |
| View 层 | `member_4_winmonitor/src/ui` | 继续补截图、演示流程和 UI 测试记录 |
| QA / Docs | `member_4_winmonitor/tests`、本文档 | 在现有 29 个测试用例基础上整理报告格式 |

### 11.15 当前进度下的收敛建议

| 问题 | 当前表现 | 一个月计划中的处理方式 |
|:---|:---|:---|
| 多套实现并存 | `member2_plant_module` 与 `member_4_winmonitor/src/core` 都有植物/状态机实现 | 第 2 周集成时确定 `member_4_winmonitor` 为主线，成员 3 的优秀实现迁移或对齐接口 |
| 数据层命名不统一 | `DatabaseManager` 与 `RandomAccessDatabase` 并存 | 成员 2 负责统一对外接口，保留课程要求最清晰的一套随机读写说明 |
| 文档已接近最终状态 | 架构、进度、自查已经写入本文档 | 成员 1 汇总结题结构，成员 5 补齐 UI 截图和测试材料 |
| 集成版承担过多 | `member_4_winmonitor` 同时包含 UI、Monitor、测试与核心逻辑 | 第 3 周拆清职责说明，不强行重构代码，文档中明确模块边界即可 |

---

## 附录：关键技术参考

### 二进制文件随机读写核心模式

```cpp
// 读取第 recordId 条记录
bool DatabaseManager::readById(uint32_t recordId, FocusRecord& out) {
    if (recordId >= recordCount_) return false;
    file_.clear();  // 清除 EOF/fail 标志（极其重要！）
    file_.seekg(recordId * RECORD_SIZE, std::ios::beg);
    file_.read(reinterpret_cast<char*>(&out), RECORD_SIZE);
    return file_.good();
}

// 原地更新第 recordId 条记录
bool DatabaseManager::updateById(uint32_t recordId, const FocusRecord& rec) {
    if (recordId >= recordCount_) return false;
    file_.clear();  // 清除状态位
    file_.seekp(recordId * RECORD_SIZE, std::ios::beg);
    file_.write(reinterpret_cast<const char*>(&rec), RECORD_SIZE);
    file_.flush();
    return file_.good();
}
```

### Windows API 前台窗口检测核心模式

```cpp
// 头文件中绝不包含 <windows.h>
// 仅在 .cpp 中：
#define NOMINMAX
#define WIN32_LEAN_AND_MEAN
#include <windows.h>
#include <psapi.h>

// 获取前台进程名
HWND hwnd = GetForegroundWindow();
DWORD pid = 0;
GetWindowThreadProcessId(hwnd, &pid);
HANDLE hProcess = OpenProcess(PROCESS_QUERY_LIMITED_INFORMATION, FALSE, pid);
if (hProcess) {
    wchar_t path[MAX_PATH];
    DWORD size = MAX_PATH;
    if (QueryFullProcessImageNameW(hProcess, 0, path, &size)) {
        // 从完整路径提取文件名（如 "msedge.exe"）
        QString processName = QString::fromWCharArray(path).split('\\').last();
    }
    CloseHandle(hProcess);
}
```

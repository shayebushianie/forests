针对你的Windows C++桌面端专注软件开发项目，结合华南理工大学课程设计规范（要求：5个以上类、2层以上继承、UI界面、随机文件读写、2000行以上代码、严格的代码注释与测试用例），我为你梳理了以下开发思路与架构设计方案。

---

### 1. 技术栈选型建议

在Windows客户端开发中，平衡开发效率、UI美观度及系统底层API的调用便利性是选型的关键。

| 维度 | Qt (QWidget/QML) | WinUI 3 (Windows App SDK) | Dear ImGui |
| :--- | :--- | :--- | :--- |
| **开发效率** | **高**（信号槽机制，GUI拖拽与代码结合） | **中**（现代，但C++/WinRT语法繁琐） | **高**（适合快速原型与游戏工具） |
| **UI美观度** | **高**（支持QSS样式表，易实现现代扁平化） | **极高**（原生Windows 11 Fluent设计） | **中**（偏向游戏风格，不易定制精美消费级UI） |
| **底层API整合** | **极佳**（纯C++开发，可无缝调用Windows API） | **极佳**（微软原生技术栈） | **良好**（需自行处理窗口生命周期） |
| **学习曲线** | **平缓**（文档极其丰富，适合学生团队） | **陡峭**（中文文档及社区生态较薄弱） | **平缓**（轻量级，但缺乏丰富的动画支持） |

#### 推荐方案：
*   **UI框架**：**Qt (基于 QWidget + QSS)** 
    *   *理由*：对于课程项目，QWidget的开发稳定性与Debug便利性较高。利用QSS（类似CSS）可以较为方便地实现类似“Forest”的圆角、阴影、渐变等现代UI效果。此外，其内置的 `QTimer`、信号槽（Signals & Slots）机制，非常利于解耦计时器与界面展示。
*   **底层数据存储**：**自定义定长二进制文件 (Binary Random Access File)**
    *   *理由*：由于课程大纲中明确要求了**"Random file processing (writing, reading, and updating)"**，使用成熟的数据库（如SQLite）可能会被判定为未满足“随机文件处理”这一底层要求。因此，建议采用C++的 `fstream`，通过设计**定长结构体**（如 `struct FocusRecord`）并配合 `seekg` 和 `seekp` 定位到特定字节进行增删改查。

---

### 2. 架构设计：MVC/MVVM解耦方案

为了让团队高效协作，避免将UI、计时和系统监听耦合在单个大类中，建议采用经典的 **MVC (Model-View-Controller)** 模式，并结合 Qt 的**信号槽机制**进行通信：

```
                    ┌─────────────────────────┐
                    │          View           │
                    │ (MainWindow / Canvas)   │
                    └────────────┬────────────┘
                         ▲       │
             UI 刷新信号  │       │ 用户操作 (如点击开始)
                         │       ▼
                    ┌────┴────────────────────┐
                    │       Controller        │
                    │    (FocusController)    │
                    └────┬──────────────┬─────┘
                         │              │
        状态控制 / 读写   │              │ 启停监听
                         ▼              ▼
           ┌──────────────────┐    ┌──────────────────┐
           │      Model       │    │  System Monitor  │
           │ (Plants/Storage) │    │  (SystemHook)    │
           └──────────────────┘    └──────────────────┘
```

#### 2.1 模块划分：
*   **Model（数据与状态）**：
    *   `FocusSession`：维护当前专注状态（未开始、进行中、已暂停、已失败、已完成）、剩余时间等。
    *   `AbstractPlant` 及其派生类：维护植物的生长数据。
    *   `DatabaseManager`：处理定长二进制文件的随机读写逻辑。
*   **View（界面展现）**：
    *   `MainWindow`：主窗口，处理按钮点击、倒计时数字显示。
    *   `GardenCanvas`：继承自 `QWidget`，负责绘制历史森林图案（使用 `QPainter` 绘制不同状态的植物）。
*   **Controller（控制中枢）**：
    *   `FocusController`：核心控制器。当收到 View 层的“开始专注”信号时，启动 Model 层的计时器，同时通知 `SystemMonitor` 开始监控；若 `SystemMonitor` 触发违规信号，则控制器将 Model 状态置为“失败”，并通知 View 播放植物枯萎动画，最后调用 `DatabaseManager` 将失败记录写入文件。

---

### 3. 核心难点突破策略（Windows API 监听）

在Windows下实现前台窗口监测和基本的反作弊，主要有以下技术路线：

#### 3.1 监听当前前台活跃窗口
*   **轻量级方案（轮询法）**：
    *   使用 `QTimer` 设定一个 500ms - 1000ms 的定时器。
    *   每次触发时，调用 Windows API：
        ```cpp
        HWND hwnd = GetForegroundWindow(); // 获取当前前台窗口句柄
        DWORD pid = 0;
        GetWindowThreadProcessId(hwnd, &pid); // 获取该窗口的进程PID
        ```
    *   通过 PID 获取进程路径或名称：
        ```cpp
        HANDLE hProcess = OpenProcess(PROCESS_QUERY_LIMITED_INFORMATION, FALSE, pid);
        if (hProcess) {
            wchar_t path[MAX_PATH];
            DWORD size = MAX_PATH;
            if (QueryFullProcessImageNameW(hProcess, 0, path, &size)) {
                // path 即为当前前台进程的完整路径
                // 将其与黑名单或白名单进行匹配
            }
            CloseHandle(hProcess);
        }
        ```
*   **高效方案（事件钩子法）**：
    *   利用 `SetWinEventHook` 监听 `EVENT_SYSTEM_FOREGROUND` 事件。此方案不需要定时轮询，当用户切换前台窗口时，Windows系统会自动回调我们的函数，效率更高且响应更即时。

#### 3.2 应对进程被强杀或绕过的反作弊思路
由于是课程项目，不建议编写复杂的驱动级保护，可以通过以下应用层方案提升健壮性：
1.  **拦截窗口关闭事件**：
    *   在专注期间，重写 Qt 的 `closeEvent(QCloseEvent *event)`，弹窗警告用户“此时退出将导致植物枯萎”。如果用户执意退出，则在退出前向二进制文件中写入一条“失败/枯萎”记录。
2.  **双进程守护（选做/加分项）**：
    *   设计一个极小的辅助控制台程序 `Watchdog.exe`。
    *   当主程序启动专注时，使用 `CreateProcess` 启动 `Watchdog` 并传入主程序的 PID。
    *   `Watchdog` 仅在后台检测主程序进程是否存在。如果主程序通过任务管理器被强制终止，`Watchdog` 捕获到该事件后，负责向本地二进制文件中追加写入“专注失败”的历史记录。

---

### 4. 项目里程碑规划（敏捷路线图）

针对独立开发或小型团队，建议将开发周期细化为三个阶段，确保每一步都有可运行的程序：

```
┌────────────────────────────────────────────────────────┐
│                   Phase 1: MVP 版本                    │
├────────────────────────────────────────────────────────┤
│ 1. 搭建 Qt 基础窗口，实现核心倒计时（QTimer）与状态切换 │
│ 2. 实现简易的前台窗口轮询监听（黑名单硬编码）         │
│ 3. 实现最基础的二进制定长文件单条记录写入              │
└───────────────────────────┬────────────────────────────┘
                            │
                            ▼
┌────────────────────────────────────────────────────────┐
│                Phase 2: 基础功能完善版                  │
├────────────────────────────────────────────────────────┤
│ 1. 丰富 OOP 继承体系（AbstractPlant -> Tree/Flower）  │
│ 2. 实现二进制文件的增、删、改、查（随机定位与覆盖更新） │
│ 3. 实现图形化“森林画布”，展示历史记录中的植物          │
│ 4. 支持黑白名单的 UI 配置与持久化保存                 │
└───────────────────────────┬────────────────────────────┘
                            │
                            ▼
┌────────────────────────────────────────────────────────┐
│                   Phase 3: 商业体验版                  │
├────────────────────────────────────────────────────────┤
│ 1. 接入 QSS 样式表，进行视觉美化（拟物化或扁平森林风）  │
│ 2. 引入基础的动画切换（如植物发芽、摇曳、枯萎的多帧图片）│
│ 3. 补充健壮性测试与边界测试，完善全套代码注释          │
└────────────────────────────────────────────────────────┘
```

---

### 5. 5人团队分工优化方案

结合课程大纲的评分重点（**代码正确性30%、设计合理性10%、UI界面15%、测试用例与现场演示30%、文档/报告20%**），对你们现有的分工进行了针对性的优化和任务细化：

#### 成员 1：项目经理与前端主架构 (PM & UI Core)
*   **技术职责**：
    *   负责基于 Qt 搭建主窗体 `MainWindow`，设计应用的主界面、专注设置页、黑白名单配置页、以及统计图表页。
    *   使用 QSS 对界面进行美化，确保满足考纲中“UI Friendly and nice visual effect (15分)”的要求。
    *   负责利用 `QPainter` 或 `QGraphicsView` 开发“森林画布”组件，读取历史记录并在画布上渲染出属于用户的虚拟森林。
*   **文档职责**：
    *   作为 PM，主导并汇总编写 **Appendix 1 (Proposal, >=1000字)** 及 **Appendix 2 (Final Report, >=3000字)**。
    *   严格把控项目进度，确保整体代码量达到 **2000行** 的硬性指标。

#### 成员 2：面向对象业务核心开发 (OOP & Logic)
*   **技术职责**：
    *   设计并实现满足考纲（多类、多层继承）的植物体系：
        *   第一层：`AbstractPlant`（抽象基类，定义纯虚函数 `grow()`、`wither()`，以及基础属性如名称、生长阶段、所需时间等）。
        *   第二层：`Tree` 和 `Flower` 继承自 `AbstractPlant`。
        *   第三层：具体植物如 `OakTree`、`PineTree`、`Rose`、`Sunflower` 继承自第二层。
    *   编写核心计时器状态机 `FocusController`，负责番茄钟各个状态的流转（Idle $\rightarrow$ Focus $\rightarrow$ Success/Failed），并通过**多态机制**在计时过程中调用具体植物的生长逻辑。
    *   按照规范，为所有的类及关键函数编写**详尽的 Doxygen 风格注释**（阐述类职责、继承关系、函数参数、核心算法步骤）。

#### 成员 3：底层数据存储开发 (Data & File System)
*   **技术职责**：
    *   **死磕随机文件处理 (Random file processing)**。严禁直接使用 SQLite 或 JSON 库，必须使用 C++ 的流操作。
    *   设计固定长度的二进制记录结构体：
        ```cpp
        #pragma pack(push, 1) // 确保结构体无字节对齐填充，便于精确计算偏移量
        struct FocusRecord {
            int recordId;          // 记录唯一标识
            char plantType[32];    // 种植的植物类型
            int durationSeconds;   // 专注时长
            long long timestamp;   // 时间戳
            bool isSuccess;        // 是否成功
            char reserve[23];      // 预留字段，方便后续升级且保持定长（如凑齐 64 字节）
        };
        #pragma pack(pop)
        ```
    *   封装 `RandomAccessDatabase` 工具类，利用 `std::fstream` 的二进制模式（`std::ios::binary | std::ios::in | std::ios::out`）提供以下底层 API：
        *   `writeRecord(const FocusRecord& record)`: 追加记录到文件末尾。
        *   `readRecord(int index, FocusRecord& record)`: 使用 `seekg(index * sizeof(FocusRecord))` 随机读取特定条目。
        *   `updateRecord(int index, const FocusRecord& record)`: 使用 `seekp(index * sizeof(FocusRecord))` 随机更新已存在的某条数据（对应考纲中的 "updating" 要求）。
        *   `getRecordCount()`: 获取当前总记录数。

#### 成员 4：系统级 API 与反作弊监控 (System Hook)
*   **技术职责**：
    *   封装 `SystemMonitor` 类。可以使用单独的子线程（继承自 `QThread`），避免阻塞 Qt 的主 UI 线程。
    *   实现黑白名单的过滤算法。当专注开始时，由主线程启动监控；监控线程定时调用 `GetForegroundWindow` 等 Windows API，判断当前处于活跃状态的窗口。
    *   如果检测到当前活跃进程属于黑名单（如游戏、浏览器等），或者超出了白名单范围，立即通过 Qt 信号（Signal）通知主线程的 `FocusController` 判定专注失败。
    *   （选做加分项）实现前述的 `Watchdog.exe` 后台守护程序，防止用户通过任务管理器强杀主程序来作弊。

#### 成员 5：测试工程师与品控 (QA & Testing)
*   **重要性**：考纲中“测试与正确性”占了 30% 的高额分值，且现场 Demonstration 表现占 20%，该成员的角色极为关键。
*   **技术职责**：
    *   设计测试矩阵，编写单元测试（可采用 `Qt Test` 框架或 `Google Test`），确保覆盖以下边界情况：
        1.  *文件读写边界*：如果数据库文件为空，读取第 0 条数据是否崩溃？随机更新第 100 条记录时，是否会错误地覆盖第 99 条或第 101 条的数据（字节对齐与偏移量计算验证）？
        2.  *断电/异常退出保护*：专注过程中如果遭遇程序崩溃或直接关闭，重新打开时数据是否保持一致？是否成功写入了“枯萎”状态？
        3.  *并发冲突*：当 `SystemMonitor` 线程在检测窗口时，主线程突然终止专注，是否存在多线程竞争导致内存泄漏或奔溃？
        4.  *计时器精度*：长时间专注时，倒计时时间是否准确。
    *   配合 成员2 检查并规范化整个项目代码的注释质量。
    *   负责答辩时的 Demo 演示排练，确保在评审老师提问和现场测试时，能够展示出系统在各种极端操作下的稳定性。
